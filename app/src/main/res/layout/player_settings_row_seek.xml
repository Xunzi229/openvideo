<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/row_title"
        style="@style/Widget.OpenVideo.PlayerSettings.RowTitle"
        android:layout_width="0dp"
        android:layout_weight="1" />

    <SeekBar
        android:id="@+id/row_seekbar"
        android:layout_width="@dimen/ov_player_settings_seek_width"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/row_value"
        android:layout_width="@dimen/ov_player_settings_value_width"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:textColor="@color/player_text_secondary"
        android:textSize="14sp" />
</LinearLayout>
