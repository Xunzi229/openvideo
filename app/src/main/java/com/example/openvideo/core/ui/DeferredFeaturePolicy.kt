package com.example.openvideo.core.ui

/**
 * 未完成功能的用户入口开关。默认全部关闭。
 * 底层代码、接口和测试保留，重新开放时只改这里并同步
 * `docs/roadmap/deferred-features.md`。
 */
object DeferredFeaturePolicy {
    const val ONLINE_SUBTITLE_SEARCH_VISIBLE = false
    const val SOURCE_FUTURE_ADAPTERS_VISIBLE = false
    const val PLAYER_CAST_VISIBLE = false
}
